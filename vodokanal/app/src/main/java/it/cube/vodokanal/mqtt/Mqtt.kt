package it.cube.vodokanal.mqtt

/**
 * Класс для взаимодействия с Mqtt протоколом:
 * Удобная абстракция для отправки и приёма последнего сообщения
 * Ну я пока хз, может это было уже реализовано в готовых библиотеках,
 * пусть будет, если что удалим.
 */
class Mqtt {

}